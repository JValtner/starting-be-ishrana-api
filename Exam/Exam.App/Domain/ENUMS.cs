namespace Exam.App.Domain
{
    public enum AppointmentStatus
    {
        Scheduled = 0,
        Cancelled = 1,
        Completed = 2
    }

    public enum UserRole
    {
        Veterinarian,
        Assistant,
        Owner
    }
}
